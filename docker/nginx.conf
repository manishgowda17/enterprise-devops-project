nginx:
  image: nginx:latest

  container_name: enterprise-nginx

  restart: unless-stopped

  ports:
    - "80:80"

  volumes:
  - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro

  depends_on:
    - web

  networks:
    - enterprise-network
